# Qdrant (Cloud or self-hosted)
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION=system_rag

# Embeddings (FastEmbed)
FASTEMBED_MODEL=intfloat/multilingual-e5-small
EMBEDDING_DIM=384

# Chunking and retrieval
CHUNK_SIZE=900
CHUNK_OVERLAP=150
TOP_K=6
MIN_RELEVANCE_SCORE=0.78

# Inference (any OpenAI-compatible chat completion endpoint)
INFERENCE_BASE_URL=
INFERENCE_API_KEY=
INFERENCE_MODEL=
INFERENCE_TEMPERATURE=0.2

# Runtime
LOG_LEVEL=INFO
LOG_FILE=logs/rag-qdrant.log

# Photo storage (description-based RAG matching).
# Photos are content-addressed by sha256 under this directory; the
# directory is created on the first photo write.
RAG_PHOTOS_DIR=/root/rag-photos

# Admins (Telegram user ids). Comma-separated. Required at import time;
# missing/empty/non-integer values are fail-closed. Replace the three
# <to_fill> placeholders with the real Telegram user ids before going
# live. The mapping is:
#   boonhian@blueacres.sg  -> <to_fill>
#   boonching@blueacres.sg -> <to_fill>
#   aloycwl@gmail.com      -> <to_fill>
# The known admin id 920567169 is included as a stable default for
# development. Admins are checked server-side; the LLM cannot bypass.
ADMIN_TELEGRAM_IDS=920567169,<to_fill>,<to_fill>

# Local @username <-> telegram_id resolver cache. Every inbound
# message records the sender so admins can reference users by
# @username in chat (e.g. "let @alice see the Q3 note"). The file
# is created with mode 0600 (owner read/write only). Relative paths
# resolve under the project root.
RAG_USER_CACHE_PATH=logs/user_cache.json

# Caching (opt-in; both disabled by default)
# Semantic cache: keyed by question similarity (cosine)
SEMANTIC_CACHE_ENABLED=0
SEMANTIC_CACHE_PATH=logs/semantic_cache.sqlite
SEMANTIC_CACHE_TTL_SECONDS=86400
SEMANTIC_CACHE_MISS_TTL_SECONDS=3600
SEMANTIC_CACHE_MAX_ENTRIES=1000
SEMANTIC_CACHE_SIMILARITY_THRESHOLD=0.88
SEMANTIC_CACHE_CACHE_MISSES=1

# Search result cache: keyed by exact question hash + settings
SEARCH_CACHE_ENABLED=0
SEARCH_CACHE_PATH=logs/search_cache.sqlite
SEARCH_CACHE_TTL_SECONDS=86400
SEARCH_CACHE_MAX_ENTRIES=5000
